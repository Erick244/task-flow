import TaskColumnAction from "./TaskColumnAction";
import TaskColumnRoot from "./TaskColumnRoot";
import TaskColumnTitle from "./TaskColumnTitle";

export const TaskColumn = {
    Root: TaskColumnRoot,
    Title: TaskColumnTitle,
    Action: TaskColumnAction,
};
